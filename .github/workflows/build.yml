name: Build Steve's Carts 3

on:
  push:
    branches: [ main, master, dev, 1.19, 1.18.2-port ]
  pull_request:
    branches: [ main, master, dev, 1.19, 1.18.2-port ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '7.6'
        cache-read-only: false
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Fix SSL settings for Linux CI
      run: |
        # Remove Windows-specific SSL workarounds that cause handshake_failure on Linux
        # (jsse.enableSNIExtension=false disables SNI which most modern Maven repos require)
        sed -i 's/^org\.gradle\.jvmargs=.*/org.gradle.jvmargs=-Xmx4g/' gradle.properties
        sed -i '/^systemProp\.jsse\.enableSNIExtension/d' gradle.properties
        sed -i '/^systemProp\.sun\.security\.ssl\./d' gradle.properties
      
    - name: Build with Gradle
      run: ./gradlew build --no-daemon --stacktrace
      
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: stevescarts-jar
        path: build/libs/*.jar
        retention-days: 30
        
    - name: Upload build logs
      uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: build-logs
        path: build/reports/
        retention-days: 7
